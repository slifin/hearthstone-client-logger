#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../file-tail/bin/file-tail" "$@"
  ret=$?
else 
  node  "$basedir/../file-tail/bin/file-tail" "$@"
  ret=$?
fi
exit $ret
